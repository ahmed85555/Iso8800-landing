# ISO 8800 Landing (Vite + React + Tailwind)

## Run locally
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```

## Deploy (static hosting)
- The `dist/` folder is static. Upload it to Netlify, Vercel (Static), GitHub Pages, or any S3 bucket / static host.
